version: '3.8'

services:
  license-fe-test:
    container_name: license-fe-test
    build:
      context: .
      dockerfile: Dockerfile
    image: license-fe:test
    environment:
      - NODE_ENV=test
    env_file: .env.test
    ports:
      - '2000:3000'
    restart: always
